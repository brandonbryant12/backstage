extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.7", import: ["@key"])

type Query {
  applicationById(id: ID!): Application
}

type Application @key(fields: "id") {
  id: ID!
  name: String
  description: String
  agileEntityName: String
}